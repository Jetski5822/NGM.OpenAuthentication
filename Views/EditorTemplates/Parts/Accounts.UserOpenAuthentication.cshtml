@model NGM.OpenAuthentication.ViewModels.OpenIdIndexViewModel
@using Orchard.Utility.Extensions;

@{var index = 0;}
<fieldset>
    <legend>Associated OpenID Accounts</legend>
        <ol>
        @foreach (var entry in Model.Accounts) {
            <li>
                @entry.Account.ExternalDisplayIdentifier
                
                <a href="@Url.Action("Delete", new { area = "NGM.OpenAuthentication", externalIdentifier = @Model.Accounts[index].Account.ExternalIdentifier, returnUrl = ViewContext.RequestContext.HttpContext.Request.ToUrlString() })" itemprop="RemoveUrl UnsafeUrl">@T("Remove")</a>
            </li>
            index++;
        }
        </ol>
</fieldset>